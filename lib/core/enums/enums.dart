enum APIState {
  init,
  loading,
  error,
  done,
}

enum PaginatedApiState {
  init,
  loading,
  error,
  done,
  loadingMore,
  errorMore,
}

enum BottomBarRoutes {
  home,
  shop,
  setting,
}

enum RoyalReelsLvlCardState {
  closed,
  land,
  landOfEnemies,
}

enum SidesEnum {
  top,
  left,
  bottom,
  right,
}

enum SizeEnum {
  small,
  medium,
  large,
  extraSmall,
  extraLarge,
}
